<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyunbindev.graffiti.mappers.FeedBaseMapper">
    <select id="getFeedRecent" resultType="java.util.Map">
        SELECT * FROM feed_base_entity
        WHERE group_id IN
		<foreach item="groupItem" collection="groupIds" open="(" separator="," close=")">#{groupItem}</foreach>
        ORDER BY id DESC
        LIMIT #{limit} OFFSET #{offset};
    </select>
    <select id="getTest" resultType="java.lang.String">
        SELECT id FROM feed_base_entity WHERE id = 1;
    </select>
</mapper>